# Job-Master · 个人秋招工作台

一个**零依赖、单文件**的秋招投递管理应用。浏览器直接打开 `index.html` 即可使用；数据以浏览器本地缓存为准，并自动同步到项目 `data/` 目录，与 Trae AI Agent 共享同一份数据、双向协同。

> 设计语言参考 Cursor CN：暖纸灰浅色主题、墨黑胶囊按钮、衬线标题、等宽眉标、翡翠绿强调色。

---

## 一、功能清单

### 核心模块

| 模块 | 功能 |
|---|---|
| 投递列表 | 记录每一条秋招投递（企业名称/部门名称/当前状态/目标岗位/所属行业/工作地点/最新日期/链接/备注）；关键词搜索（企业/岗位/部门/地点）；按行业、状态筛选；表内直接下拉改状态 |
| 进度看板 | 8 个阶段（待投递 → 简历筛 → 笔试筛 → 专业面 → 主管面 → HR面 → Offer / 已挂）横向看板；拖拽卡片即变更进度并自动追加备注历史 |
| 邮箱收件 | 展示 Agent 经 IMAP 脚本采集的邮件池；未读圆点标记待处理邮件；一键「创建投递」自动清洗企业名并预填；可忽略无关邮件 |
| 数据管理 | 投递漏斗统计（累计/近 7 天/各状态/Offer/行业分布，数字滚动动画）；目录同步状态面板；CSV 导入/导出（按更新时间行级合并）；清空本地缓存 |

### 字段预设与自定义（2026-09-15 更新）

- **当前状态**：`待投递 / 简历筛 / 笔试筛 / 专业面 / 主管面 / HR面 / Offer / 已挂`，新增「待投递」作为投递前状态，看板首列
- **所属行业**：预设 `银行 / 制造 / 车企 / 互联网 / 央企 / 证券`；表单下拉中选择「＋ 新增行业…」即可自定义，新行业持久保存并自动加入下拉列表（Agent / CSV 导入写入的新行业也会被自动收录）
- **工作地点**：预设 `杭州 / 上海 / 广州 / 天津 / 合肥 / 北京 / 深圳 / 南京`；表单中复选多选，表格内渲染为彩色地点标签，数据以 `;` 分隔存储（如 `上海;北京`）
- **最新日期**：与该投递最新进展相关的日期，可为未来日期（如笔试/面试安排）

### 数据与同步架构（2026-09 最新更新）

**本地缓存优先，三层持久化：**

1. **浏览器本地缓存（第一优先）**：所有变更实时写入 `localStorage`，即使同步未启用数据也绝不丢失；
2. **定时自动保存到本地目录**：每 **60 秒**、切换标签页、关闭/刷新页面前，自动把缓存中的变更冲刷写入数据目录（目录授权模式写 CSV 文件；HTTP 模式 PUT 到 `./data/`）；
3. **外部变更自动合并**：页面获得焦点 / 每 60 秒自动检测 `data/` 目录中 Agent 或脚本的外部修改，按「更新时间较新者胜」做行级合并。

**两种目录同步通道（自动选择，目录授权优先）：**

| 通道 | 触发条件 | 读 | 写 |
|---|---|---|---|
| HTTP 同步（默认） | 页面经本地/预览服务器访问 | 自动 GET `./data/`，零配置 | PUT 写回；服务器不支持时自动降级为**只读同步**并在指示器提示 |
| 目录授权同步 | 用户在「数据管理」中授权 `data/` 目录（File System Access API） | 自动 | 完整读写，刷新后仍保持授权 |

### 其他能力

- 侧边栏可折叠/拖拽调宽；**收起后隐藏箭头，点击品牌 Logo 即可复原**
- 表格状态列渲染为彩色状态胶囊（8 种状态 8 色）；行业与工作地点渲染为彩色标签 pill，自定义行业/地点自动配色
- CSV 导出带 BOM，可直接用 Excel 打开；导入自动按更新时间合并去重，旧版表头（type/appliedAt）自动迁移为 industry/latestDate
- 全部操作有 Toast 反馈（成功 ✔ / 失败 ⚠）

---

## 二、UI / UX 设计

**设计基调：Cursor CN 式的克制与编辑感。**

- **浅色主题**：暖纸灰底 `#F6F6F4` + 纯白面板 + 极细边框 `#E9E9E4`，多层克制的阴影体系
- **双色策略**：墨黑 `#191918`（主按钮、导航选中 pill、品牌 Logo）× 翡翠绿 `#10B981`（强调、成功态、焦点光环）
- **字体系统**：正文系统无衬线；页面标题与统计大数字用衬线体（Georgia/宋体系）；眉标、时间戳、同步状态用等宽字体（如 `TRACKER · 2026.09.15 TUE`）
- **品牌标识**：墨黑渐变圆角方块 + 白色 ✔（寓意「确认进度」），favicon 同款
- **自绘 SVG 图标集**：18+ 枚图标全部手绘，统一 `stroke-width=1.5`、圆角线帽，与品牌 ✔ 一脉相承
- **微交互**：
  - 视图切换淡入上移（staggered fadeUp）
  - 统计数字滚动动画（ease-out cubic）
  - 看板卡片悬停浮起、拖拽列高亮光环
  - 模态框弹性入场、遮罩渐入
  - Toast 右下角带图标滑入
  - 按钮按压回弹、Logo 悬停微放大

---

## 三、技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 载体 | 单文件 `index.html` | 零外部依赖、零构建，双击即用 |
| UI | 原生 HTML/CSS/JS | CSS 变量主题系统；事件代理；模板字符串渲染 |
| 持久化 L1 | `localStorage` | 投递数据 + UI 偏好（折叠/宽度/当前视图） |
| 持久化 L2 | File System Access API | 目录授权模式；句柄存于 IndexedDB，跨会话恢复 |
| 持久化 L2' | fetch GET/PUT | HTTP 模式对接 `./data/`；Last-Modified 变更检测 |
| 定时器 | setInterval / setTimeout 防抖 | 60s 定时落盘；输入防抖 500ms 写回；焦点回归 800ms 合并 |
| 数据格式 | CSV（UTF-8 BOM + CRLF） | 标准转义；全量读取 → 内存修改 → 覆盖写回 |
| 采集脚本 | Python 3.8+ 标准库 | `scripts/fetch_emails.py` IMAP 拉信，零第三方依赖 |

**数据文件：**

- `data/jobmaster_data.csv` — 投递主数据（唯一真相）。列：`id,company,department,status,position,industry,location,latestDate,link,source,notes,createdAt,updatedAt`；id 规则 `jm_时间戳_4位hex`；`industry` 预设 银行/制造/车企/互联网/央企/证券（支持自定义），`location` 多值以 `;` 分隔（预设 杭州/上海/广州/天津/合肥/北京/深圳/南京），`status` 含「待投递」共 8 个枚举
- `data/emails.json` — 邮件池。每封含 `processed / matchedEntryId / matchedAction` 处理状态回写

---

## 四、使用方式

1. **打开应用**：双击 `index.html`（Chrome/Edge），或经本地服务器访问（如 `python -m http.server 8765`）——后者自动启用 `./data` HTTP 同步；
2. **记录投递**：点「新增投递」手动录入，或在 Trae 中对 AI 说「帮我同步今天的秋招邮件」「我投了 XX 帮我记录一下」由 Agent 代写；
3. **协同工作流**：浏览器改动 → 定时自动写回 `data/`；Agent 修改 CSV → 页面获得焦点时自动合并，双方始终共享同一份数据。

---

## 五、未来功能计划

| 方向 | 设想 | 优先级 |
|---|---|---|
| 统计可视化 | 漏斗转化率图、投递趋势周/月折线、企业类型占比环形图 | 高 |
| 日程提醒 | 笔试/面试时间提取并生成日历（ICS 导出/系统通知） | 高 |
| 邮箱自动化增强 | 页面内直接触发 IMAP 拉取；邮件智能匹配已有投递而非仅新建 | 高 |
| 面试准备 | 按企业/岗位关联题库、面经笔记、复盘记录 | 中 |
| 多端同步 | 可选 WebDAV/云盘通道，跨设备同步 `data/` | 中 |
| PWA 离线 | Service Worker 缓存 + 桌面快捷方式安装 | 中 |
| 导出升级 | 投递周报（Markdown/PDF）、Excel 多 sheet 导出 | 中 |
| 主题扩展 | 跟随系统深色模式；强调色可切换 | 低 |
| 数据守护 | 本地缓存版本快照，误删一键回滚 | 低 |

---

*最后更新：2026-09-15*
